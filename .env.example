# Base URL only — no trailing slash, no /rest/v1
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# Fan control mode: auto | on | off (managed via the web dashboard; defaults to auto)
FAN_MODE=auto
